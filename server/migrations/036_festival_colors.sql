-- Цвет номера на экране показа. До этого он выводился из названия команды —
-- удобно, пока команд мало, но на 22 участниках соседние номера получали почти
-- одинаковые оттенки, и разобрать их на круге было нельзя.
--
-- Теперь цвет задаётся руками из палитры «Персоны Лета». Пусто — прежнее
-- поведение, цвет по команде.
ALTER TABLE festival_participant ADD COLUMN IF NOT EXISTS color TEXT;
