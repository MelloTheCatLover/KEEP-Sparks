-- Праздничное оформление живёт дольше самого события: сам день рождения —
-- одна дата, а украшенный сайт хочется оставить ещё на несколько дней.
--
-- Отдельная колонка, а не растянутый end_date: даты смены — это даты смены,
-- на них завязаны дни и ростер, и двигать их ради оформления нельзя.
-- NULL = как раньше, по последний день смены.
ALTER TABLE shift_info
  ADD COLUMN IF NOT EXISTS festive_until DATE;
