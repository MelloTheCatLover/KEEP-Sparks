-- Публикация финальных итогов. Пока флаг снят, экран зрителя показывает
-- живое табло, как и раньше: места по ходу гонки ещё меняются, судьи вносят
-- баллы, админ правит ошибочные отметки. Итоги объявляются один раз — кнопкой,
-- когда все правки внесены, и только тогда зритель видит итоговое место.
ALTER TABLE festival_race
  ADD COLUMN IF NOT EXISTS results_published BOOLEAN NOT NULL DEFAULT FALSE;
