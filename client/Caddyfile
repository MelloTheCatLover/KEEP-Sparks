# SPA served on :80 behind Dokploy/Traefik (Traefik terminates TLS and routes
# the domain: /api -> backend, everything else -> this static frontend).
# The client calls a same-origin "/api" (VITE_API_BASE_URL default).
:80 {
	encode gzip
	root * /srv
	try_files {path} /index.html
	file_server
}
